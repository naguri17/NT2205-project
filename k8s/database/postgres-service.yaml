---
apiVersion: v1
kind: Service
metadata:
    name: postgres
    namespace: database
    labels:
        app: postgres
        component: database
spec:
    type: ClusterIP # Internal only - not exposed externally
    ports:
        - port: 5432
          targetPort: 5432
          protocol: TCP
          name: postgres
    selector:
        app: postgres
